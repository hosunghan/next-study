import LocalMain from "@/domains/next-functions/font/local";
import React, { FC } from "react";

type Props = {};

const LocalPage: FC = (props: Props) => {
  return <LocalMain />;
};

export default LocalPage;
